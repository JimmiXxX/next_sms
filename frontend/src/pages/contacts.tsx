import { Header } from "../other/header/header";

const Contacts = () => {
    return (
        <>
            <Header></Header>
            <h1>Это контакты</h1>
        </>
    );
};

export default Contacts;