import { Header } from "@/widgets/header/header";

const Contacts = () => {
    return (
        <>
            <Header></Header>
            <h1>Это контакты</h1>
        </>
    );
};

export default Contacts;