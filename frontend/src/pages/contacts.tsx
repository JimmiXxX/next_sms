import { Header } from "../widgets/MainPage/header/header";

const Contacts = () => {
    return (
        <>
            <Header></Header>
            <h1>Это контакты</h1>
        </>
    );
};

export default Contacts;